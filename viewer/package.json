{
  "name": "gaia-viz-viewer",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=iife --platform=browser --target=es2020 --outfile=dist/main.js",
    "check": "tsc --noEmit",
    "dev": "esbuild src/main.ts --bundle --format=iife --platform=browser --target=es2020 --outfile=dist/main.js --watch --serve=8000 --servedir=."
  },
  "dependencies": {
    "regl": "^2.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.3",
    "typescript": "^5.8.3"
  }
}
